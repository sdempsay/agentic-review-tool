---
paths:
  - "**/pom.xml"
---

# Pom formatting rules

Apply these rules whenever you create or edit `pom.xml` files.

If a pom file is changed, it must be made tidy by running `mvn tidy:pom` after it has been saved.