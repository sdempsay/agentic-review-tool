<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.dempsay.maven</groupId>
    <artifactId>dempsay-parent</artifactId>
    <version>1.0.4</version>
  </parent>

  <groupId>org.dempsay</groupId>
  <artifactId>code-review</artifactId>
  <version>1.1.0-SNAPSHOT</version>
  <packaging>jar</packaging>

  <name>Code Review Pipeline</name>
  <description>Cost-effective agentic code review CLI using langchain4j</description>
  <url>https://github.com/sdempsay/agentic-review-tool</url>
  <organization>
    <name>Dempsay.org</name>
    <url>http://www.dempsay.org</url>
  </organization>
  <licenses>
    <license>
      <name>MIT License</name>
      <url>https://opensource.org/licenses/MIT</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <properties>
    <exceptional.version>1.0.9</exceptional.version>
    <jackson.version>2.18.2</jackson.version>
    <langchain4j.version>1.17.2</langchain4j.version>
    <slf4j.version>2.0.16</slf4j.version>
    <snakeyaml.version>2.3</snakeyaml.version>
    <maven-shade-plugin.version>3.6.0</maven-shade-plugin.version>
    <junit.version>4.13.2</junit.version>
    <picocli.version>4.7.6</picocli.version>
    <maven-antrun-plugin.version>3.1.0</maven-antrun-plugin.version>
    <dist.dir>${project.build.directory}/code-review-dist</dist.dir>
    <dist.archive>${project.build.directory}/code-review-${project.version}.tar.gz</dist.archive>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-bom</artifactId>
        <version>${langchain4j.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>
  <dependencies>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-ollama</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.langchain4j</groupId>
      <artifactId>langchain4j-open-ai</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-nop</artifactId>
      <version>${slf4j.version}</version>
    </dependency>
    <dependency>
      <groupId>org.dempsay.utils</groupId>
      <artifactId>exceptional</artifactId>
      <version>${exceptional.version}</version>
    </dependency>
    <dependency>
      <groupId>info.picocli</groupId>
      <artifactId>picocli</artifactId>
      <version>${picocli.version}</version>
    </dependency>
    <dependency>
      <groupId>org.yaml</groupId>
      <artifactId>snakeyaml</artifactId>
      <version>${snakeyaml.version}</version>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>${junit.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>${maven-shade-plugin.version}</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <finalName>code-review</finalName>
              <transformers>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                  <mainClass>org.dempsay.codereview.CodeReviewApplication</mainClass>
                </transformer>
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <id>generate-bash-completion</id>
            <phase>package</phase>
            <goals>
              <goal>exec</goal>
            </goals>
            <configuration>
              <executable>java</executable>
              <arguments>
                <argument>-jar</argument>
                <argument>${project.build.directory}/code-review.jar</argument>
                <argument>generate-completion</argument>
              </arguments>
              <outputFile>${project.basedir}/bin/code-review_completion</outputFile>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>${maven-antrun-plugin.version}</version>
        <executions>
          <execution>
            <id>assemble-code-review-dist</id>
            <phase>package</phase>
            <goals>
              <goal>run</goal>
            </goals>
            <configuration>
              <target>
                <mkdir dir="${dist.dir}"/>
                <copy file="${project.build.directory}/code-review.jar" tofile="${dist.dir}/code-review.jar"/>
                <chmod file="${project.basedir}/bin/code-review" perm="ugo+rx"/>
                <copy file="${project.basedir}/bin/code-review" tofile="${dist.dir}/code-review"/>
                <chmod file="${dist.dir}/code-review" perm="ugo+rx"/>
                <copy file="${project.basedir}/bin/code-review_completion" tofile="${dist.dir}/code-review_completion"/>
                <copy file="${project.basedir}/dist/config.json" tofile="${dist.dir}/config.json"/>
                <copy todir="${dist.dir}/rules">
                  <fileset dir="${project.basedir}/rules"/>
                </copy>
                <tar destfile="${dist.archive}" compression="gzip">
                  <tarfileset dir="${dist.dir}" prefix="code-review" filemode="755">
                    <include name="code-review"/>
                  </tarfileset>
                  <tarfileset dir="${dist.dir}" prefix="code-review" filemode="644">
                    <exclude name="code-review"/>
                  </tarfileset>
                </tar>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
